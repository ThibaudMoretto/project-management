{
  "name": "the-best-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "project-management-types": "1.0.1"
  }
}